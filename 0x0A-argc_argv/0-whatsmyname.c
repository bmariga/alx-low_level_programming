#include "main.h"
#include <stdio.h>

/**
 * main - prints its name followed by new line
 * @argc: holds arguments passeed
 * @argv: pointer array for arguments
 *
 * Return: Always (0) success
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
