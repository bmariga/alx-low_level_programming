#include "main.h"
#include <stdio.h>

/**
 * mul - function
 * description - multiplies two integers
 *
 * @a: first input
 * @b: second input
 *
 * Return: always (0) success
*/

int mul(int a, int b)
{
	return (a * b);
}
